// DEPRECATED — renamed to ManagerStandardsTab.
// This shim is left in place because the local file system blocks deletion;
// run `git rm src/components/ManagerBibleTab.tsx` to clean up before push.
export { default } from './ManagerStandardsTab';
